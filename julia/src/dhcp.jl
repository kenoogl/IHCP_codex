module DHCP

"""Placeholder for direct heat conduction problem solvers.
Julia versions of `coeffs_and_rhs_building_DHCP`, `assemble_A_DHCP`, and
`multiple_time_step_solver_DHCP` will be implemented here."""

function coeffs_and_rhs_building_DHCP(args...)
    error("coeffs_and_rhs_building_DHCP not yet ported to Julia.")
end

function assemble_A_DHCP(args...)
    error("assemble_A_DHCP not yet ported to Julia.")
end

function multiple_time_step_solver_DHCP(args...)
    error("multiple_time_step_solver_DHCP not yet ported to Julia.")
end

end # module DHCP
